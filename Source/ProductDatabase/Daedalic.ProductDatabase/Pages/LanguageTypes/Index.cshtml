@page
@model Daedalic.ProductDatabase.Pages.LanguageTypes.IndexModel

@{
    ViewData["Title"] = "Language Types";
}

<partial name="_Alert" />

<h1>Language Types</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>

<partial name="_Filter" />

<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-page="./Index" asp-route-sortOrder="@Model.SortOrders["name"]">
                    @Html.DisplayNameFor(model => model.LanguageType[0].Name)
                </a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.LanguageType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
