@page "{id:int}"
@model Daedalic.ProductDatabase.Pages.Platforms.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Platform</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Platform.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Platform.Name)
        </dd>
    </dl>

    @if (Model.Platform.Releases != null && Model.Platform.Releases.Count > 0)
    {
        <div class="alert alert-warning" role="alert">
            This platform has @Model.Platform.Releases.Count releases associated with it. Deleting the platform will delete all releases as well.
        </div>
    }

    <form method="post">
        <input type="hidden" asp-for="Platform.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
